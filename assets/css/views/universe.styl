@import 'nib'
@import '../utils/config'

@keyframes pulseStar
  0%
    opacity: 0
  100%
    opacity: 1

.universe
  position: relative
  width: 100%
  height: 100%
  //perspective: cameraDistance
  //Only for domPaint mode
  .object
    user-select: none
    position: absolute
    animation: pulseStar 1s ease-in-out 0s infinite alternate